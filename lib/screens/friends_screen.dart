import 'package:flutter/material.dart';

class FriendsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Друзья')),
      body: Center(
        child: Text('Система друзей отключена'),
      ),
    );
  }
}
